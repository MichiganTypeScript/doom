from Andarist:

```ts
const andarist = (x: number) => {
  let a = x + 10
  if (a > 5) {
    a = a + 2
  }
  a = a + 7 
  return a
}
```

```c
int andarist(int x) {
    int a = x + 10;
    if (a > 5) {
        a = a + 2;
    }
    a = a + 7;
    return a;
}
```
